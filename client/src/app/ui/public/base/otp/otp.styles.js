import classNames from 'classnames';
import styles from '../../../_app.scss';

export default {
  OTP: classNames(styles['otp']),
  HEADER: classNames(styles['header']),
  ICON_LEFT: classNames(styles['icon-left']),
  ICON_RIGHT: classNames(styles['icon-right']),
  PROGRESS_BAR: classNames(styles['progress-bar']),
  LINE_PROCESS: classNames(styles['line-process']),
  DONE: classNames(styles['done']),
  GROUP_LOGO: classNames(styles['group-logo']),
  TITLE: classNames(styles['title']),
  LOGO: classNames(styles['logo']),
  INFO: classNames(styles['info']),
  OUTER_INPUT: classNames(styles['outer-input']),
  FIELD_INPUT: classNames(styles['field-input']),
  ERROR: classNames(styles['error']),
  ERROR_MESSAGE: classNames(styles['error-message']),
  GROUP_BTN: classNames(styles['group-btn']),
  LINK: classNames(styles['link']),
  LINK_RESEND: classNames(styles['link-resend']),
  DISABLED: classNames(styles['disabled']),
  OUTER_INPUT_ERROR: classNames(styles['outer-input-error']),
}
