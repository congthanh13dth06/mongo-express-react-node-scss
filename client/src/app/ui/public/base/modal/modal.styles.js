import classNames from 'classnames';
import styles from '../../../_app.scss';

export default {
  MODAL: classNames(styles['modal']),
  MODAL_OPEN: classNames(styles['modal-open']),
  MODAL_DIALOG: classNames(styles['modal-dialog']),
  MODAL_CONTENT: classNames(styles['modal-content']),
  MODAL_HEADER: classNames(styles['modal-header']),
  MODAL_BODY: classNames(styles['modal-body']),
  MODAL_FOOTER: classNames(styles['modal-footer']),
  MODAL_BTN_CLOSE: classNames(styles['modal-btn-close']),
  MODAL_LABEL: classNames(styles['modal-label']),
  MODAL_LOGO: classNames(styles['modal-logo']),
  MODAL_TEXT: classNames(styles['modal-text']),
}
