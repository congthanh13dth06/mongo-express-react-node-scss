import classNames from 'classnames';
import styles from '../../../_app.scss';

export default {
  INPUT_CONTAINER: classNames(styles['input-container']),
  LABEL_INPUT: classNames(styles['label-input']),
  GROUP_INPUT: classNames(styles['group-input']),
  INPUT: classNames(styles['input']),
  ERROR: classNames(styles['error']),
  ERROR_MESSAGE: classNames(styles['error-message']),
  HAS_ICON_LEFT: classNames(styles['has-icon-left']),
  HAS_ICON_RIGHT: classNames(styles['has-icon-right']),
  ICON_LEFT: classNames(styles['icon-left']),
  ICON_RIGHT: classNames(styles['icon-right']),
  TOOLTIP: classNames(styles['tooltip-group']),
  TOOLTIP_TEXT: classNames(styles['tooltiptext']),

  DROP_DOWN_LIST: classNames(styles['drop-down-list']),
  GROUP_SEARCH: classNames(styles['group-search']),
  SEARCH_LIST: classNames(styles['search-list']),
  ICON_SEARCH: classNames(styles['icon-search']),
  LIST_ITEMS: classNames(styles['list-items']),
  SCROLL: classNames(styles['scroll-bar']),
  ITEM: classNames(styles['item']),
  ACTIVE: classNames(styles['active']),
  SELECT: classNames(styles['select']),
  GROUP_INPUT_DIAL: classNames(styles['group-input-dial']),
  DIAL_PHONE: classNames(styles['dial-phone']),
  DISABLED: classNames(styles['disabled']),
}
