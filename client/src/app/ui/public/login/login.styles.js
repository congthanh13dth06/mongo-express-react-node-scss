/**
 * Created By Nguyen Cong Thanh on 03/04/2019 15:46.
 *
 * Copyright Intelin 2019.
 */

import classNames from 'classnames';
import styles from '../../_app.scss';

export default {
  PUBLIC: classNames(styles['public']),
  CONTAINER: classNames(styles['container']),
  LOGIN: classNames(styles['login']),
  HEADER: classNames(styles['header']),
  HEADER_ICON_LEFT: classNames(styles['icon-left']),
  HEADER_ICON_RIGHT: classNames(styles['icon-right']),
  LANGUAGE:classNames(styles['language']),
  HEADER_LANGUAGE_ACTIVE: classNames(styles['language-active']),
  PROGRESS_BAR: classNames(styles['progress-bar']),
  LINE_PROCESS: classNames(styles['line-process']),
  FORM_INPUT: classNames(styles['form-input']),
  LOGO: classNames(styles['logo']),
  GROUP_BUTTON: classNames(styles['group-btn']),
  LINK: classNames(styles['link']),
  LINK_FORGOT: classNames(styles['link-forgot']),
  LINK_REGISTER: classNames(styles['link-register']),
  POWER_BY: classNames(styles['power-by']),
  FORM_ACCOUNT_LOCKED: classNames(styles['form-account-locked']),
  FORM_ACCOUNT_LOCKED_TITLE: classNames(styles['title']),
  FORM_ACCOUNT_LOCKED_IMAGE: classNames(styles['image']),
  DESCRIPTION: classNames(styles['description']),
  DISABLED: classNames(styles['disabled']),
}
